# 工作方法

这个项目两天里由五个会话并行建成。下面是被**至少一个真实 bug** 付过学费的规则——
每条都带案例,案例都在 git 历史里。没有付过学费的偏好不收录。

## 一、语料定理(被验证七次的那一条)

> **夹具和代码出自同一个信念,所以夹具只会确认、不会检验。
> 只有别人写出来的文件能反驳我们。**

| # | 案例 | 形状 |
|---|---|---|
| 1 | id 白名单拒绝真实 ST 的全部 31 个聊天文件 | 每条测试用自己生成的 id,规则作者脑中的形状 |
| 2 | `TavernHelper_scripts` 夹具把 `value` 编成字符串 | 照键名摘要臆造,测试通过了自己的臆造 |
| 3 | 导轨断言 `key === 'a1'` | 注释正确,oracle 是假客户端的真值 |
| 4 | 世界书统计 50 倍误差 | 把"ST 显式写出的默认值"数成"用户改过" |
| 5 | 预设哨兵反了 | 夹具用的正是代码在找的那个哨兵 |
| 6 | 用户行被写上回复的状态树 | 夹具作者不知道 ST 会往用户行放变量,所以永远构造不出那种夹具 |
| 7 | jQuery 之谜 | 读注入侧漏一个源;**消费者自己的构建配置**(必须正确的那一侧)一锤定音 |
| 8 | 模型三连不吐 UpdateVariable | mvu-flow 夹具照 `_.verb()` 老方言写、全绿;真卡说的是 `<JSONPatch>`。语料同卡 1/1 合规 vs 我们 0/3,差异只能在我们发的内容里——顺藤摸出**两个**缺口(宏未展开+方言不认),一个致盲一个致聋,单看任何一侧的测试都是绿的 |

推论:
- **优化提案也和"别人的存储"同源**——先量自己写的是什么,再决定优化什么(MVU 90% 冗余写)。
- **上一个样本与预期一致,不能省掉这次取样**;一个样本与信念一致,更不能(66% → 23%–92%)。
- **旧结论形状吻合时也要重新取样**(两次"沙箱红"同形状、不同原因)。

## 二、计数课

1. **数标签不是数行为**(声称 289 处依赖恒等 escape,实测行为依赖是 3 处)。
2. **diff 只告诉你"不同",不告诉你"和什么相同"**("被用户改过的模板"逐字节等于另一份出厂文件)。
   同族的基线陷阱两例:`default/content/settings.json` 是首存落盘的内存默认,拿它当基线
   会虚报 233 个"用户改动";拿存在的键去比缺席的键必然报"不同"(群聊设置假阳性)。
   共同形状:**diff 的另一边是什么,和 diff 本身一样重要**。
3. **包含不是调用**(`</system_instruction>` 里数出 12 处 `/sys`;救法永远是把命中原文打出来读)。
4. **数字必须带口径**("量到 2 处"实为"我的尺子只量得到 2 处";`saveSettings` 25 处 →
   限定宿主对象后 ST 真调用 0 处,结论反转)。
5. **口径不是"你能量什么",是"下游要拿它算什么"**(普查"文件里有多少 HTML"数字全对,
   下游要的是"渲染器要做多少事",两者之间隔一次渲染器加工——按后者重量,差 15 倍且
   集中度反转。比分类错更难自查:输出完全合理,分布检查也过。抓到只因存在一个界面上
   明晃晃的反例。救法:给下游当输入的测量,先问一句"你要拿它算什么"再动手。
   同一课重犯的成本随下游距离放大:第三次栽在这课上时,错的数字已经进了协调者的
   规划——"语料无验收样本"的结论量的是盘面,按渲染后口径实为 179 楼 11 张卡,
   渲染管线一直有充足语料;基于它做出的"向用户要样本卡"的请求,理由整个不成立)。
6. **一个结论依赖几层,就得读几层**(同日四案,四人份的学费:主文件 0 命中而消费点在
   prompt-converters.js;grep 命中行号真实、却落在不会执行的 convertCohereMessages 里;
   读全了 calcToRender、漏了它的下界由 ST 的 chat_truncation 另一系统决定;读了外层
   await、漏了内层 try 把拒绝接住)。三种漏法:**跨模块**(消费点在另一个文件)、
   **跨系统**(我读的函数里那个边界值由别人决定)、**跨嵌套**(外层的语义被内层改写)。
   搜索是它的特例,两个方向都要:**"0 命中"≠不存在;"有命中"≠在路径上**——命中要读
   它所属的函数。过滤器版:**一个过滤器的否定结论,强度不超过它的召回率**;收紧过滤器
   排除假阳性时,同时检查排除了什么真阳性(词边界排掉 `.` 连带排掉了全部 `对象.方法`
   形状,两个"没人用"翻案)。而且**召回率不足不是让结论变弱,是让结论变成另一个结论**
   (1 张卡 → 3 张卡,把"边缘用法可以不管"变成了"两种是探测,问题问错了")。
   两个方向的错同时存在是最难从输出发现的配置——它们部分对冲成一个"看起来合理"的
   数字,既非上界也非下界;且可发现性不对称:**虚高自曝(表里多出的行会被人问),
   漏判自藏(漏掉的不产生任何行)**——所有静默失败都长在"不产生输出"那一侧。可执行的两半:范围小于整个相关子系统时,**把范围写进结论本身**(产出
   可检查);下结论前问一句**"这个结论依赖几个源,我读了几个?"**(自查不依赖形状——
   条目只能拦见过的形状,而"把避错句写进文档、又在同一份文档另一节没对自己套用"当天
   就发生了一次:知道规则不构成防护)。
7. **转述二手数字时,该跟着抄的是口径,不是数字。** 同伴报「对重建的 677 表剪 **311**/338」,
   我把 311 抄进了验收单。后来它被撤回 —— **不是算错了,是那次实验测了一条_产品里不存在的
   路径_**(对全局做一次扫描;上游只有有界窗口 `[old−2−2·keep, old]`)。改按真实路径量,
   同一局**开启那一刻只剪 21 层**。
   **危险在于 311 长得很像一个正确的大数**:量级合理、与总数 338 自洽、任何自检都过 ——
   它满足了"数字该有的样子"的每一条,唯独**被量的那条路径不存在**。这与本节第 5 条是同一课
   (口径错的正确计数),但**转述这一步把它放大了**:原报告者至少知道自己跑了什么,
   而我只拿到结果。**抄一个数时,我实际继承的是它的口径,却往往只复制了它的数值。**
   可执行:**引用同伴的数,连同"这个数是哪条路径/哪个对象产出的"一起引**;
   写不出那半句,就说明我还不能用它。**副产品:写不出口径这件事本身,就是该回去问的信号。**

## 三、静默失败纪律

> **一切失败必须有声音;没有声音的成功比失败更糟。**

- **拒绝要抛、要点名成员/主机/命令,并随身携带证据**("语料对此命令调用数为 0")。
  返回 `undefined` 或 `{}` 是把策略决定伪装成数据缺失——最坏的一例:空对象被 MVU 读成
  "未初始化"而拿默认值覆盖真实状态,"不是失败,是先毁掉数据再成功"。
- **用户看不见的警告等于没有警告**(整个 `warn` 通道默认阈值下是黑的;CSP 拒绝被卡作者的
  "你的酒馆坏了"盖过)。拒绝必须盖过 fallback。
- **宁可缺,不能错发**(`MESSAGE_SENT` 从 `stream.start` 推会在重生成时错发;拦截型事件
  当通知转发 = 执行卡的修改、显得成功、然后丢弃)。
- **hang 是唯一不自己开口的失败**——给它装超时判据,消息带上它在等的 URL。
- **担保"有错才回调"的接口,要先问它怎么拼写"没错"**(ws 的 send 回调在成功时传 `null`,
  `!== undefined` 的守卫把 370 次成功上报成 370 次失败,全部打印成 `null`;第一版解释自信地
  冤枉了 logger,照那个解释做的"修复"在下一次成功发送时把宿主整个带崩——崩点才是真诊断。
  建立在未验诊断上的修复,实际上是一次没被承认的实验)。
- **没展开的宏原文上线,全程无声**(`{{format_message_variable::stat_data}}` 字面量发给模型,
  模型被要求给看不见的变量打补丁,于是整块无视——症状出现在三层之外的"模型不合规"上)。
  凡是"替换失败退回原文"的机制,退回那一步都需要自己的声音。
- **解析期失败只能从外面看**(语法错误杀掉整个脚本块,frame 内的 try/catch 来不及存在),
  所以入口完整性在注入前查,两道门:构建期查产物、注入前查取到的东西。
- **一个不依赖失败本身的解释,是用自信语气说出的猜测**(硬编码的"假客户端拒绝"把人送去
  调一条已经通了的传输)。
- **误报的仪器比没有仪器糟**(StrictMode 排练被报成真 unmount;jQuery UMD 按加载环境分叉,
  检查器断言了生产永远不走的分支)。修法:**只问这个环境答得了的问题**。
- **"哪些代码在报告边界内"要系统地问**(publishGlobals 在 try/catch 之外失败,无人叙述)。

## 四、验证纪律

- **一条断言只有在它是唯一失败的那一条时才算验过**(teeth-check 三次才验对)。
- **验证结果证实了你本来就怀疑的结论时,要比它推翻你时更用力地检查**
  (坏正则"确认"了错误观察;25 处 saveSettings"支持"了合理假设——救回来的都是反常感,
  而反常感是运气不是方法)。
- **接口文档里语气最肯定的那条,正是下游最该追问的那条**(带"最容易踩"标签的警告
  自己两个方向都错;追问成本一行类型错,不追问则错到接线且不报错)。
- **实现和可达是两张列表**(五个 handler 全部实现、测试、且在 wire 上不可达)——
  行为测试直调 handler 表是对的,正因如此可达性要有自己的守卫(源码守卫 + 真 socket 守卫)。

## 五、工具与形状

- **重复踩同一个坑时,换工具而不是修这次的输入**(shell 吃反斜杠 ×4;第四次起
  用 `String.fromCharCode` 或重设计成不需要转义——后者往往本来就是更好的设计)。
- **隐藏的默认值把决定藏起来,出错时指向错误的责任人**(`source` 缺省=假数据宣称、
  `networkGranted` 缺省=授权像坏了、frame 的 `mode` 必填)。
- **派生值不存储**(profile 显示名存成快照 → "叫 deepseek 实指 gemini";现算的 summary
  让不一致在构造上不可能;同一原则:身份铸造、不推导)。
- **手维护的列表需要守卫,守卫要自维护**(probe 表:没有 probe 的方法直接判失败)。
- **白名单编码了写下它的那天**(rpc-client 遗漏)——每张白名单要么被测试盯着,要么在腐烂。
- **共享工作树上,`git add -A` 是把别人的进行时当成自己的完成时**(四次:两次扫走同伴
  的半成品、一次把并发写的重复 arm 提交、第四次把一位同伴**正在编辑且套件红着**的六个
  文件连红带错扫进仓库——每次都发生在"这批文件明明有清单"的时刻)。规则:协调者提交
  **只按报数清单显式列文件**,无清单的文件一律留在树里;`&&` 链会在套件红时照样走到
  commit,判决行要先读再落,不是先落再读。
  (补记,由被扫的那位指出:上面这条初版写的是"永不 add -A"——**针对手法而不是依据**,
  `git add packages/` 会造成同样后果而绕开字面。规则的锚是"只暂存报数清单逐条列出的
  路径";配套另一半与暂存方式无关:**落批前确认树绿**——协调者看到的工作树里没有任何
  东西区分"写完了"和"写到一半",所以"改到哪一步"这个判断必须由报数者的绿字承担,
  报数从此自带文件清单与已跑绿声明。清单本身也有失效方式:报数者列出的是"我认为
  改过的",需要的是"实际有差异的",两者靠记忆桥接——而记忆是这条链上唯一不可复现的
  环节。清单由 `git status --porcelain` 生成,不由回忆生成。)
- **沙箱要从卡手里没收的能力,必须先自己握住**(`postMessage` 在重定义 parent 前绑定;
  构建断言:全 bundle 恰好出现一次)。
- **最具体的诊断优先**——同一份坏输入可以让三条判据同时为真,只有第一条告诉读者该改什么
  (HTML 回退先于"被转换"判定)。
- **通道可以通用,权限不行**(`call {method}` 一条帧,白名单由 shell 执行——
  frame 对"什么可调"的看法只是不受信一侧的提议)。
- **上游源码里"看似多余"的每一行都是有人流过血的地方**(`structuredClone` 防父会话被改;
  照抄前先问它防什么)。

- **两个正确的一半不等于一个正确的整体**(宿主 forget 了授权,浏览器缓存短路不再问——
  两半各自有测试、各自绿、合起来仍然漏)。与"没执行"类不同:这类**都执行了但被短路**;
  跨信任域的修复要一条端到端测试跨过两半。
- **内容按名字重新绑定,权限不重新绑定**——删卡是 characterId 唯一一次换主人的时刻;
  任何新的 per-character 存储写之前先回答"它是内容还是权限"。

- **测试要能和写它的人意见相左**(一条叫 "legible" 的测试断言 token 的**名字**,调色板换值
  它全程绿——选颜色的判断力写进断言,自然不会反对自己;从 token **算出**比率的测试才会)。
  同族:把 1.34 修到 1.66 是"穿着修理外衣的微调"——修要修到**有依据的线**(WCAG 3:1),
  不是挪过自己上次的败点 0.07。
- **"只有旁边有更亮的才看得见,就是看不见"**——相对可见性不是可见性。

- **便利品的偏离若恰好抵消另一个偏离,就从"简化"变成"隐藏"**(fake 的 delete 不清授权 +
  id 用永不复用的计数器铸造:第二条把第一条藏得任何观察都绿——没有症状、一次改动就复活、
  且让整类缺陷在开发期不可能被看见)。fake 的每个偏离要**单独**核对"它挡住了什么被观察"。
  顺带:**同一个组件里两种做法并存,不一致本身就是线索**——其中一种多半是图省事加的。

## 六、OVERLORD 战役的三课(一天内十二道门,每道各付一次学费)

- **读属性的检查看不见形态**(lodash-es 命名空间有全部方法、每个都能用、对象不可调用,
  `typeof value.get === 'function'` 全程绿——检查要**按消费者的确切用法调用它**,
  `value([...]).map(...).last()` 才会红)。同族:等值断言 `=== 'no-cache'` 会把更严的
  `no-store` 也判死——断言"不许说回去"(`!includes('max-age')`),不断言当前值。
- **判决要能被撤回,且撤回留痕**(15 秒判死抛弃了它取消不了的 import;bundle 迟到、
  跑通、发布,而面板还在替一张能跑的卡报丧。改法不是删判决:迟到成功补发
  "loaded, but only after 17s"——两半都要,悄悄刷成 loaded 会把那十七秒死寂从记录里
  抹掉;常驻报告标 withdrawn 不删除,frame 确实说过那句话)。
- **在长 TTL 路由上,发错的头比这次部署活得久;curl 永远看不到坏副本,因为 curl 没有
  浏览器缓存**(无 ACAO 的 200/七天副本让此后每轮 CORS 校验对着尸体失败;宿主侧怎么
  查都健康。响应的正确性依赖一个会变的头时,它就不该被持有到头变化之后)。

## 七、文档的分界

**有些句子是决定,有些是测量,只有决定放在散文里是安全的。** 决定靠意图为真,代码不符
是代码的错;测量只对一个时刻为真,而散文无法察觉自己过期,新鲜与陈旧的测量排版完全
一样。所以**每一条被行为依赖的测量前提,都要配一个会自己失败的东西**——文档讲道理,
测试押前提(范例:DEVIATIONS §3 的结论完全依赖"4 张监听卡全自带 bundle"这个语料
事实,那个事实不是引文,是一条语料不再支持就变红的断言)。付费实例:ROADMAP 指着
几天前完成的工作、RENDER 的两条前提先后被撤回、"结构性无样本"量的是盘面。

## 八、更正的惯例

错了的句子**划掉保留**,写明错在哪、怎么发现的,不偷偷替换(PLAN 的哨兵、fork 前提、
注入清单、ARCHITECTURE 的"未验"句)。更正显示过程,过程比结论便宜十倍——
是因为有人把它写下来了。

### 理由句和结论句可以指向相反的方向,而中间没有任何摩擦

一条账本注释写着:「上游把 Esc 当成永久拒绝——**用户以为自己推迟了,上游却认为他永久
拒绝了**——所以壳必须把关掉弹窗映射成永久拒绝。」

**那个破折号里的句子是「不要复现」的论据,而结论是「照抄」。** 两句挨着写、一次落笔,
通读时毫无异样:理由是真的,结论也是一个成立的选项,只是它们指向相反的方向。

> **读自己写下的判断时,把理由句和结论句分开读一遍,只问一件事:它们指同一个方向吗。**

这个形状可以 grep:凡是「因为 X 会伤害用户,所以我们照做」的段落都值得停一下。它之所以
难自查,是因为**每一句单独看都对**——错的是它们之间那个「所以」,而句子级的复核不检查
连接词。

## 九、检查动作自己也有未被检查的前提

同一课在一天里从三个方向撞出来,三次都不是"结论错了",而是**得出结论的那个动作本身
依赖着一个没人验过的前提**。

**第一面:探针之间的干扰不会报错,它会产出一张看起来完整的表。**
测"哪些访问器把活对象交给卡"时,六个探测共用一个 realm 顺序跑。前面那个探测 push 了
一条消息,把后面 `getSwipes()` 指向的楼层换掉了——七行里错两行,**方向相反**
(`getAllVariables` 报成活的,实际是拷贝;`getSwipes` 报成拷贝,实际是活的)。改成
每个探测一个全新 realm 才对。要点是:那张表自身没有任何地方显示"这两行不可信",
**我是因为其中一行和我读代码的预期矛盾才回头查的——没有独立预期,那张表会被直接发出去。**

**第二面:召回不足的检测器,输出和"确实没有"完全一样。**
同一天,量具第一版只搜 `script.js` 一个模块,于是把 `extensionSettings` 报成非活对象;
更早一版的匹配边界排除了 `.`,于是 `TavernHelper.getWorldbook(...)` 这类正当调用被漏掉,
两个成员因此被归进"未建且没人用"——而那一栏正是用来决定**不做什么**的。

**第三面,也是最锋利的一句:零是唯一一种全盲检测器也会给出的答案。**
所以任何"我们搜了,没有"都必须先证明这把卡尺看得见东西:造一个已知该命中的样本,
确认它命中;造一个已知不该命中的,确认它不命中。3e 的 `mustSee` / `mustNotSee` fixture
和 `test-collection` 的自检都是这个形状。

**链条**(每一环都比上一环更靠近工具本身):

1. **断言** —— 断言写错(同义反复、`x <= max(y,x)` 恒真)。
2. **输入** —— 断言对,夹具错(`'/a\/b/'` 塌成纯文本,于是"转义分隔符"用例里没有转义分隔符)。
3. **破坏动作** —— 夹具对,牙检的变异根本没生效(CRLF 文件上用 `\n` 锚点),而
   "没有测试变红"和"我压根没破坏任何东西"输出相同。
4. **测量之间的隔离** —— 每个探测都对,探测之间互相污染。

**共同形状**:每一层的防御动作都**消耗掉了本该用来怀疑这个防御动作的注意力**。做完牙检
产生的那份踏实感,恰好来自牙检,又恰好挡住了对牙检的怀疑——它是自指的,所以没有外部
视角时基本不可能自破。这也是为什么这三面是**互相**发现的,而不是各自发现自己的:
需要的不是"另一个人",是**一个不共享你当前注意力焦点的视角**。

**可操作的替代品**(不必等到有人来看):任何强迫你把「我做了什么防御」和「那个防御
依赖什么」**分开写下来**的动作。列出"哪条红哪条绿"的过程本身就在问"凭什么"。

### 第五环:根本没有测试在看(2026-09-03,宿主域)

上面四环都假设**有一个测试在看着这段代码**。若没有,四环全部为真而整件事仍是空的。

实证:把 `lineSystemFlags` 整个函数体换成 `() => false`,**974 条测试全绿**。它此前
零断言覆盖,于是那次重构在套件眼里根本不存在——"全绿"对它一个字都没说。

**而覆盖率抓不到这一处。** `--experimental-test-coverage` 报我域里 82–98% 的函数覆盖,
`lineSystemFlags` 一直算"已覆盖",因为 `turnForMessage` **每次都会执行它**——只是没有
任何断言依赖它的返回值。

> **覆盖率数的是「有没有被执行」,要问的是「有没有被断言」。这两件事可以完全不相干。**

于是同一个病有三张脸,而且**互相盖不住**:

| 形态 | 现象 | 为什么它骗得过人 |
| --- | --- | --- |
| 夹具错 | 牙齿**沉默** | 测试与牙检互相印证对方的沉默 |
| 无测试 | 牙齿**不存在** | 破坏代码也不红,和"改动是对的"输出相同 |
| 用覆盖率去查 | **量错了对象** | 它对前两者都不报警,而且给出一个体面的百分数 |

**可操作**:重构完、报告之前,先问"套件看得见这次改动吗",并且用**破坏它**来回答,
不要用覆盖率回答。若没有东西变红,交付物就不是那次重构,而是一条钉在**它真正决定
什么**的层面上的测试——`lineSystemFlags` 只决定一件事:`'latest'` 要跳过末尾的
system 行。补的就是那一条。

(照这条把域里另外三个"安静做决定"的函数也变异了一遍——`prunedNote`、`embeddedHash`、
`placementFor` 三个都红,所以零断言是例外不是常态。变异扫描便宜到可以当例行动作:
改成常量、跑一次、还原。)

## 十、测量的适用范围会被悄悄扩大一格

`ScriptChatMessage` 用 SillyTavern 的存盘字段名,依据是"语料里 194 次 `context.chat`
访问读的就是 `mes` / `is_user` / `swipes`"。**那个测量是对的。** 而它被用在了
`TavernHelper.getChatMessages()` 上——**另一个面,上游在那里做了改名**。于是卡读
`.message` 拿到 `undefined`。

**测量正确、结论正确、适用范围被悄悄扩大了一格。** 这比测错更难发现,因为复核测量
本身永远查不出问题:数字是真的,方法是对的,错的是它管到了哪里为止。

配套的两句反向提醒:

- **"命中 ≠ 在路径上"**(别名追踪把卡自己的局部 `ctx` 算成上游成员,34 员虚高到实际 13)。
- **"0 命中 ≠ 不存在"**(见第九节第三面)。

三句是一组:一个测量可以在**数值**上正确,同时在**范围**、**召回**、**归属**上任意一项
出错,而这三种错都不会让数值看起来可疑。所以引用一个测量时,除了"多少",还要写下
**"量的是哪个面"**。

## 十一、三种判别力:断言、样本、被测对象

「能通过」不等于「能证明」。判别力可以在三个位置各自丢失,而且三处的输出都一样是绿色。

**一、断言没有区分力。** 断言恒真(`x <= max(y,x)`),或者断言的性质对正确实现和最像的
错误实现都成立——`RegExp.prototype.source` 会把正斜杠重新转义,所以"正确的 unescape"
和"坏掉的 unescape"渲染出来一模一样,断言它等于同义反复。

**二、样本没有区分力。** 全局世界书那次:18 张卡各 +15 条,读起来是很强的验收,而
"修好了"和"重复了"**都会让数字变大**。判别力全部集中在**第 19 张**(绑了全局书、应当
+0 的那张)。**样本量不等于判别力**——该问的不是"我有多少样本",是"哪些样本上,正确
实现和最像的错误实现会给出不同答案",这一问的答案常常是个位数,有时是 1。

**三、被测对象自己免疫。** 最隐蔽的一种,因为它看起来是**最真实的用例**:真卡、真调用。
ERA 主包那个提取器 `mes` / `swipes` / `message` 三分支全兼容,所以形状对错它都活;
拿它验收,通过率 100%,判别力 0。**防御性写的代码天然免疫于形状错误。** 挑样本时,
三分支兼容的提取器和单分支硬依赖的脚本,判别力天差地别——而前者往往看起来更"典型"。

**四、牙齿检查自己也会没有判别力,而它是最容易被当成已验证的一种。** 变异了实现、跑了
测试、**没红**,通常读作「这条测试盖不住这个分支」;但还有第二种可能:**样本没有那个
分支要处理的性质**。

一个具体的:说明符遍历里有一条守卫「说明符不能跨行」,防的是截断的包体把下一行代码
吞成一个巨大的说明符。我把守卫删掉,测试全绿。看起来是「没测到」,其实是我那条测试的
源码在截断的说明符**后面没有第二个引号**——守不守,遍历都因为找不到收尾引号而跳过。
断言为真,而且两种实现下都为真。补上一行 `const after = "hello";` 之后立刻红。

> **变异不红有两种解释:测试没断言这件事,或者样本给不出这件事。** 两者的修法相反——
> 一个加断言,一个换样本——而输出一模一样。

所以牙齿检查的完整形式是三步,不是两步:改坏它、看红、**如果没红,先问样本能不能走到
那一步**。这和 §十九、二十四是同一个家族:判别力丢在样本那一层时,所有指标都正常。

**同一天里同一课三次**(说明符遍历,2026-09-03):

| 拆掉的守卫 | 第一版夹具 | 为什么什么都没证明 | 能咬人的夹具 |
| --- | --- | --- | --- |
| 「说明符不能跨行」 | `import "url⏎const after = 1` | 后面没有第二个引号可吞,两版都跳过 | `…⏎const after = "hello";` |
| 正则的字符类 | `/[/]/;import "x"` | 提前收尾只剩 `]/;`,走查照样恢复 | `/[/"]/;import "x"` ——**类里必须有引号** |
| 「`from` 必须跟子句尾」 | 四个真包体 | 字符串跳过已经抓掉全部 23 条 | `const re = /from"/;…` ——字符串跳过**结构上到不了**的那一个上下文 |

前两行是「夹具里没有那段代码要保护的东西」。**第三行是另一种,而且更值得学**:一个新守卫
的变异什么都不改,这是关于**它在哪里挣到饭吃**的证据,不是它无用的证明——去找另一层
**结构上到不了**的那个上下文;找不到就删掉它。两种情况都要在报告里说清**是哪一层干的活**,
否则下一个读者无从判断。

同族的还有:验收标准本身可以没有区分力。「四处不再抛 + console 干净」这条标准,
恰好会给一个**中间态**发通行证——`.message` 修好而 `.data` 仍缺失时,那张卡从"抛"
变成"静默读错楼的状态",标准全部满足,而我们刚把一个响的故障换成了哑的故障。
**验收标准要能拒绝中间态**,否则它奖励的是"看起来修好了"。

推论:挑验收样本时,**先说出"如果我改错了,哪个样本会红"**;答不上来的样本不要用,
它只会给你信心。

## 十二、修缺口之前,先量自己

四次同形,四天之内:

- 派了四个 `SillyTavern` 成员,查完**三个已通**(代理会落到快照字段上)。
- `setExtensionPrompt`:契约、handler、消费方全在,**只缺门面那一跳**——四分之三已建。
- `chat.splice` 想找宿主 arm,以为要新建,**`chat.deleteMessage` 早就在**(和删整聊天的
  `chat.delete` 是两个东西)。
- 楼层变量:以为两半缺口都要新传输,d7 一查——**数据两天前就在 frame 里了**
  (`chat[i].variables[swipe_id]`)。

共同形状:**我们对上游做测量,对自己做的是记忆,而记忆里的仓库是几天前的那个。**
一个多 session 并发推进的树,每个人心里那份"我们建到哪了"都在以小时为单位过期。

所以:**在设计一个缺口的填法之前,先花一次 grep 确认它还是个缺口。** 这一步的成本是
分钟级,跳过它的成本是设计一件已经存在的东西——而且那种设计往往会**建出第二份**,
于是两份实现开始漂移(`getChatMessages` 的两套形状就是这么来的:一份在
`iris-compat-tavernhelper`,一份在浏览器门面,同一个成员,字段名不兼容)。

## 十三、判别力的第四种:清单的完整性

前三种(断言 / 样本 / 被测对象)问的都是"这个证据能不能区分两种实现"。第四种问的是
**"我看的是不是全集"**,而它的输出形状最有欺骗性:

> **一份不完整的清单和一份完整的清单,长得一模一样——都是一份看起来像全集的名单。**

一条为了让 `SillyTavern` 面"可见"而写的钉住测试,拿一个**手写的候选名单**去过滤。于是
它只能报告**有人想到过的名字**——26 员里钉住 18。这正是本仓库最老的那条("清单只能
谈论它上面有的名字")**出现在清单自己的守卫里**。

**第一次修比第二次更值得记**:改成从 `CARD_METHODS` + 快照的键**派生**候选,多抓到三个,
看起来解决了。但派生的来源是**共享 fixture 的快照**,而那份 fixture 没有五个字段。
**候选集从"我想到的名单"降级成了"fixture 恰好有的字段"——仍然是一个不完整的样本,
只是不完整得不那么明显。**

而且:**"我已经改成派生的了"这句话本身会让人停止怀疑完整性。** 一个看起来更系统的方法,
会比一个明显手工的方法更难被继续追问——这是第九节那条("防御动作消耗掉本该用来怀疑
防御动作的注意力")在完整性上的实例。

第二次修的做法:用一个**带齐全部契约字段**的快照来探,并在注释里写明这些字段为什么是
**写死的而不是从 helper 展开的**——从 helper 展开等于把完整性外包给一个可能变瘦的东西。

### 附:两个人数同一个面,差一员,两个都对

3e 数出 25(声明面:`ScriptContext` 12 字段 + `CARD_METHODS` 13),我数出 26(应答面:
再加上代理自己回答的 `getContext` / `eventSource` / `event_types` / `updateChatMetadata`,
减去已进 deny list 的)。

**这不是谁错了,是"面"这个词有两个所指**:声明了什么,和读得到什么。一个代理可以回答
它没有声明的名字,也可以拒绝它声明过的名字。**差额本身就是面的一部分**——它恰好等于
"代理自答"这一类,而那一类是最容易在重构里被整批改掉的。

所以引用一个成员数时,和第十节同一条纪律:除了"多少",还要写下**"数的是哪一种面"**。

## 十四、更正链:一个正确的测量是怎么被改错的

字体去重那件事的完整链条,值得整条留着,因为**每一环都是一个不同的教训,而单看任何一环都学不到另外两个**。

1. **d7 量产物,得 399,597 —— 对的。**
2. **我量了源(`node_modules/.../all.min.css`),得 799,056,并"更正"了他 —— 错的。**
   那个文件这个构建根本不导入;导入的是拆分子表。而**同目录下 `message-preset-styles.ts` 的注释里就写着这件事**,包括 `all.min.css` 有 10 个块。我读了 vite 配置,没读那个文件。
3. **协调者以中继身份把这个更正转成了权威指令。**
4. **d7 据此把自己原本正确的文档改错了。**

### 三个教训,分属三个环

**更正要过和原测量同样的卡尺。** 一次"更正"天然带着权威气场——它出现在第二位,姿态是纠偏,而且通常比原结论更具体(我给了 799,056 这个精确数字)。**但更正本身是一次新的测量**,它可以用错的口径、错的样本、错的文件。原测量要过的那道门,更正一步都不能少走。

**已写下的教训不自动生效。** 我自己的 memory 里就躺着一条「measure through the product's own reader」——量产物,不量源——而这次违反的正是它,产物还是我几分钟前刚构建出来的。第九节那句「防御装置比再记一遍教训有效」在这里的对应物是具体的:**一条更正必须附带"我量的是哪个文件"**,让它自己暴露口径,而不是指望作者在正确的时刻想起正确的原则。

**权威转发不是验证。** 中继环节把"某人说"变成了"已裁定",而它没有增加任何证据。**中继放大错误的速度比产生错误快**——原作者至少还有机会怀疑自己,收到指令的人往往不会去重测一个已经被裁过的结论。所以中继一条更正时,该带上的是**它的证据**,不只是它的结论。

### 附带抓到的一件

`message-preset-styles.ts` 那段注释说拆分子表之后"every face is inlined once"。产物不同意——`v4-font-face` 用另一个家族名重新声明了同样三个文件。**那是没人押的那半句**(见第七节):同一段注释里"拆分子表把 10 降到 7"被验证过,"每份一次"没有,于是它错着活了很久。**一段文字里的两句话,可以一句有测试押着、一句没有,而读者分不出来。**

## 十五、报告的价值取决于它不报什么

`$.fn` 那层缺口报告器**不报**三类读:`in` 查询、`Object.prototype` 成员、符号属性。

理由不是省事,是**信噪**:

- **检查是提问,不是使用。** `'draggable' in $el` 是一张卡在**做正确的事**——它在降级之前先问。给它记一条缺口,等于把"这张卡处理得很好"报告成"这张卡出问题了"。
- **`toString` / `Symbol.iterator` 是运行时自己的读**,任何字符串化或迭代一个 jQuery 对象的代码都会触发。卡一个都没要。报了它们,真正的信号会被埋在没人产生的流量底下。

一般形式:**一条什么都报的通道,和一条什么都不报的通道,信息量同样是零。** 前者看起来更负责,所以更难被质疑——而它的代价是把稀有的真信号推到读者不会读到的位置。

**设计报告通道时,"不报什么"和"报什么"同等重要,并且更容易写错**:漏报是缺陷,滥报是"更安全"的姿态,而两者都让通道失效。

## 十六、会静悄悄跳过测量的检查

给 frame 的全局清单加一个名字 `EjsTemplate`,就把二十六个 Tavern Helper 绑定
全部错位了一格——名字和值是两份手写清单,**按下标配对**。第十个名字碰上了第一个
helper 的值,往后全体平移。

这类故障在边界上没有症状:名字在,值是个可调用的函数,类型检查全绿,只有卡片
自己的行为错了——`getChatMessages` 执行的是隔壁那段代码。测试套件是以**挂死**
的形式发现它的,不是以不匹配的形式。

于是我写了一个不共享那个数组的检查:虚拟 parent **按名字**解析成员,所以拿
`published[name]` 和 `parent[name]` 比,任何平移都会红。用两份字面量互相比是
毫无判别力的——它们正是那两个可能互相不一致的东西(这条已经在"守卫不能与被守卫
者同源"里记过,这次是差点又犯)。

**新的一课在第一次运行:那个检查只比了 26 个名字里的 4 个。**

大多数 helper 成员不在 parent 上,而在 `parent.TavernHelper` 下,所以循环里那句
`if (onParent === undefined) continue` 静静地跳掉了 22 个。检查是绿的。它会一直
绿——而它测的东西几乎是空的。

救回来的不是我读代码,是我在循环后面加的一句下限:

```ts
assert.ok(checked.length >= 20, `only ${checked.length} names were compared: ...`)
```

**凡是带跳过分支的检查,都要对"实际比了多少"下断言。** 这和"样本量不等于判别力"
不是同一条:那条说的是样本挑得不对,这条说的是样本会在**运行时**自己缩到零,而
断言数量、通过率、绿色勾勾全都不变。一个 `continue` 就是一个静默的减样本。

顺带,这个下限写成下限而不是钉死的数:新成员该让它涨,只有**掉下去**才意味着
检查失去了触及范围。钉成等号会把"加了成员"和"检查瞎了"报成同一种红。

### 写在注释里的假设不是代码的性质

检查修好后第一次真跑,立刻抓到 `triggerSlash` 和 `getScriptId` 在两条路径上
**不是同一个对象**:`helperNames` 会过滤掉 `core` 已经绑过的名字,于是只有这两个
跳过了所有兄弟都要过的 detach 层——卡片裸调 `triggerSlash` 拿到的是宿主活对象,
同一张卡片调 `parent.TavernHelper.triggerSlash` 拿到的是克隆。

而 `helperNames` 上方的注释写着:"两者行为相同,谁赢不改变卡片看到的东西。"

这句话从写下的那一刻起就是错的。它不是一个性质,是一个**没人验过的断言**,而且
它比沉默更糟:它劝退了每一个可能去核对的读者。现在那段注释记的是这个结论为什么
成立(core 那两格改从 `tavernHelper` 取值),而不是断言它成立。

同一天的另一半也是这个形状:frame 的缺库横幅报着 `EjsTemplate` 不存在,而它已经
被我建好了——**假报告比缺报告更贵**,它派一个 peer 去找一个不存在的缺口。两条出路:
让报告变准,或让报告说的事变真。这次选后者,因为它同时更贴上游。

## 十七、断言掉进了被测对象的 try 里

`sandbox-frame.test.ts` 有八十多个测试,其中很多这样写:

```ts
evaluate(scope, globals => {
  assert.equal(typeof bare['setExtensionPrompt'], 'undefined')
})
```

那个箭头函数**跑在卡片正文该跑的地方**——也就是沙箱自己的 try/catch 里面。生产上
这是对的:卡片抛错必须被报告,不能把 frame 带走。代价是:**断言失败被当成"卡片抛
错"接住了,posted 里多一条 error,测试通过。**

一个只在 body 里断言的测试,**永远不会失败**。

我不是读代码发现的,是探到的:往 body 里塞一句 `assert.equal('a','b')`,跑,绿。

修法是 `evaluate` 把 `AssertionError` 抓出来、`send` 之后再抛;别的抛错留在里面
(有几个测试就是故意从 body 抛,考的是"卡片自己的失败会被报告")。修完全跑,
**八十三个里有一个真的一直在红**——那一条钉着 `setExtensionPrompt` 不存在,而我早就
把它实现了。它靠这个机制绿了很久。

**通例:断言必须在被测对象的错误处理之外求值。**凡是把回调交给被测代码去调的测试
形状(sandbox、事件总线、中间件、`try` 包着的插件宿主),都要先探一次:塞一个必假
的断言,看它红不红。红了才说明这一整批测试有判别力。

### 齿检自己也要有基线

同一天第二次:我给按钮成员写了齿检脚本,三个变异全都"被三个测试抓到"——看着很有
牙。其中两个测试**在未变异的树上就是红的**(我刚把 `getScriptButtons` 从桩改成实现,
它们还钉着"要报 gap")。

**一个变异的证据是它新增的失败,不是当次的失败列表。**脚本现在先跑一遍基线,并且
只报增量;基线不干净就直接说"下面每一行都读不了"。

### 检测器要和被检测者对账

同一个脚本的第一版把 TAP 的 `not ok` 当失败标记,而 node 这里用的是 spec reporter
的 `✖` ——于是每个变异都报 IMMUNE,**包括一个绝不可能免疫的**。第二版加了"解析出的
条数要和 node 自己的 `ℹ fail N` 相等",这条对账立刻又抓到自己:spec reporter 每条
失败印两遍(行内 + 末尾汇总),解析数是真值的两倍。

三次都是同一个形状,和第十六节那条是一家:**报告"没发现问题"的路径,必须先证明它
能发现问题。**

### 两层守卫的保证无法归因

`getScriptButtons` 我写了 `.map()` 复制一份再交出去,还给它写了个测试。变异掉那个
`.map()`,测试**照样绿**——因为 `createFrameTavernHelper` 返回的是过了
`detachReturns` 的表面,每个调用结果都被 `structuredClone` 过。两层都在同一个工厂
里,**没有任何一条测试路径只经过其中一层**。

我第一版把那个测试命名为「at the member and not only at the wrapper」——这是一个
关于**归因**的断言,而测试根本没有做出这个断言的能力。改掉了:现在它钉的是契约
(卡片改不到宿主的表),并且写明**删掉任意一层它都还是绿的**,所以它只挡"两层都被
删",不能证明任何一层活着。

`.map()` 留着,理由换成了一个真实的调用者类:**同文件内的组合**(append / update 那
两个糖是建在未 detach 的 api 上的)。留下防御可以;把"我防御了"写成一条测试不能兑现
的断言不行。

## 十八、桩藏得住签名错误

`replaceScriptButtons` 当桩当了很久,签名是 `(buttons)` —— **一个参数**。上游是
`replaceScriptButtons(script_id, buttons)`,例子就写在它自己的 3.2.5 更新日志里。

所以任何按上游写法调用的卡片,它的 script_id 会落在 `buttons` 上。而桩的实现是:

```ts
replaceScriptButtons: (buttons: unknown): void => {
  void buttons
  reportButtonGap('replaceScriptButtons')
},
```

**一个 no-op 没有办法告诉你它被调错了。**它把参数丢掉,报一句"这个成员没建",然后
一切看起来正常——报告甚至是准确的。错的不是报告,是**它报的那件事掩盖了另一件事**:
成员没建 vs 成员的接口是错的,前者盖住了后者,而且盖了多久没人知道。

这一条比"桩不报错"更窄也更实用:**桩的签名从来没有被任何调用检验过**,因为它对任何
输入都做同一件事。桩越"安全"(什么都不做、什么都不抛),它藏住的签名错误就越彻底。

**通例:桩转真的第一件事是核签名,而不是核行为。**核对象是上游的声明或它自己的
例子,不是我们当初写桩时对它的印象——那个印象正是桩的来源,拿它核桩是同源自检。

顺带,这一批还有一个同族的小发现:

### 一个决定有两道守卫,外面看不出来

`appendInexistentScriptButtons` 里我写了 `if (added.length === 0) return`,注释说得
也对(没有新东西就不该写)。但 `writeButtons` 已经有等值守卫,而 `[...current]` 正好
等值——所以变异检查报告:**没有任何测试能分辨这个 early return 存不存在。**

不是测试的漏洞,是那行代码本身是多余的。删了,留一处决定"没变就不写"。

判据很干净:**如果一个变异删掉某段代码而没有任何测试变红,先问它是不是多余的,
再问测试是不是不够。**上一节那个「两层守卫无法归因」是同一个形状的另一半——那次
两层都要留(理由是有一类调用者只经过一层),这次一层就够。区别是**有没有一个真实的
调用者只走其中一条路**。

## 十九、判别力的第五种:来源分叉在哪一层

前四种问的是证据能不能区分两种实现、样本够不够、被测对象会不会自己吞掉判定、清单是不是
全集。第五种问的是**来源**:

> **两条路径一致,只在它们真正分叉的那一层上有鉴别力。**

同一轮里,同两个人(3c 与语料域),一次强佐证一次空佐证:

- **硬的**:MVU 的清理参数。语料域从**磁盘上快照楼的分布**反推出间隔 50 / 保留 20;
  3c 从 `settings.json` **正读**出同一组数。两条路在"参数是多少"这一层确实不同源,
  一致有鉴别力。
- **空的**:已剥空楼层的字节数。两人**各自**算出 26244 B——计算层独立且都对。但两人
  **各自**把它放进一张为 8.29 MiB 建的 MiB 列里、各自 `toFixed(2)`、各自得到
  `0.03 MiB`,然后**各自从那个已四舍五入的显示值往回读成"约 30 KB"**。真值 **25.6 KiB**,
  高了 17%,两条路都没看见。

**失效点在引用,不在计算。** 共用的不是中间产物(没有共用),是**呈现约定**。所以第一版
诊断("别共用中间产物")开出的是错药方——那条在这里根本不适用。

### 第二面:一条路径自己也有分叉层

同一条规律对单个来源成立:**一个来源的读数,只在它真正区分的问题上有鉴别力。**

MVU 的设置对象把 `快照保留间隔` / `要保留变量的最近楼层数` / `触发恢复变量的最近楼层数`
放成同一父键下的同族名字。读设置文件能拿到三个**值**,但**区分不了"哪个值管哪个行为"**:
把它们分开的是 `restore_variables.ts` 里两处不同用法——第三个只出现在提前 return 的
**触发**判断里,而回溯**深度**用的是第二个。并成一族之后,回溯范围会算错一倍。

不是读得不仔细,是**这条路径对这个问题没有鉴别力**。要靠读消费点才能分开。可以直接当断言用的形式:

> **"结构把东西按名字归到一起"不是它们行为相似的证据。**

设置文件、类型定义里的联合类型、同一命名空间下的函数,全适用。

### 先问它区分不了什么

第二面的自查是一句问法,而它同时是**正面用法**——知道一个来源答不了什么,就知道该再找哪条路:

| 来源 | 区分不了 |
| --- | --- |
| 设置文件 | 用途(它按名字组织,不按用途) |
| 磁盘分布 | 为什么是这个值 |
| schema 默认值 | 这台机器有没有改过 |
| 类型声明 | 运行时(`get_role` 声明三值,运行时四值,第四个是 `'unknown'`) |
| 一张表的格式 | 量纲差三个数量级的两行 |
| **一个 API 的实现** | **哪些消费者走这条路** |

最后一条换了维度:前五条是"对某个**属性**没有鉴别力",这一条是"对**谁在用它**没有鉴别力"。
实现能告诉你它做什么,告不了你谁在调它——那要去数调用点。3c 读了酒馆助手
`getVariables({type:'message'})` 的 `is_system` 谓词,拿它推断 MVU 的行为,而 MVU 走的是
自己的 `isMvuData` + `findLastIndex`:**同名场景,两条路径,两个谓词。源读全了,错在假定了唯一性。**

这一条两个方向都会犯,而且错法不对称:**只读实现会高估覆盖面,只数调用点会错判语义**
(`renderEntry` 首命中即 `return`,所以"可达 16 条"不是"一趟调 16 次")。

但**别把"高估覆盖面"读成"虚惊一场"**——覆盖面和严重性是两个量,不同向:

> **从错的实现去预测,你连带继承了那个实现的失败形态;而没有任何理由说继承来的那个更糟。**

3c 那次正好反过来:按错谓词预测的失败形态是"**答空表**"(可能触发下游空值检查),真实形态是
"跳过用户行落到上一轮 AI 行,**答陈旧的 stat_data**"——形状完整、通过 schema 校验、看起来
完全健康,然后被 MVU 的读→合并→写持久化下去。**面估大了,害估小了。**

清理参数那次三条路都在,正好各答一个不同问题——而**只有 schema 能答"这是不是普遍情况"**
(答案:自动清理默认开,所以"导入的聊天已被清理过"是每个 MVU 装机的常态)。前两条结构上答不了。

### 三条操作规则

1. **要进正文的数,从数值读,不从显示读。** 表格的格式是给扫读用的;一个会出现在句子里的
   数,需要它自己的一次未舍入读取。
2. **按这个数自己的量纲格式化,不按表的量纲。** 26 KB 放进为 8.29 MiB 建的列里必然丢一位
   有效数字;逐行选单位就直接显示 `25.6 KiB`,那 17% 没有产生的机会。
3. **报一致时,说清两条路在哪一层不同源。** 不说清,读者(和作者自己)就会把空佐证当强佐证。

顺带一条分层的收益,补第八节:**分层不只让收回变成局部操作,也让定位变成局部操作。**
25.6 KiB 那个数同时出现在事实层和推论层,推论不受影响(两个数都是"小到不值得裁"),
**但事实层的数要单独改**——当初若只写在推论句里,现在得连推论一起重写。收回是知道错了
之后的动作,定位是知道错了之前的;**后者更早,也更常用**。

### 四条写代码的机械规则(宿主域 49,2026-09-03)

四条都来自同一类失败:**动作出了错,而所有门禁都是绿的**。它们不是风格建议,是动作规则。

1. **含转义序列的源码,不经 heredoc 写。** 用 Write/Edit 工具,或用
   `String.fromCharCode` / `chr()` 拼出来。

   两次实证:写 `sanitize-filename` 的 C0/C1 字符类时,`'\u0000-\u001f'` 落盘变成了**真的
   控制字符**;做键序表时 `join('\u0000')` 落盘变成了**真的 NUL**。**两次 typecheck 都过、
   测试都全绿、行为都完全正确**——发现它们的是 `grep` 说 "binary file matches"。
   第二次发生在我**刚刚为第一次写完"不要这样做"的注释之后**,在另一个文件里。

   **第三次实证:写这条规则的时候又犯了一次。** 把上面那两个转义序列写进本文档时,
   heredoc 里的转义又塌了一层,python 把它当成真的转义、于是**一条讲"别把控制字符写进
   文件"的规则,自己带着三个控制字符落了盘**,而且照样写入成功、照样没有任何门禁出声。
   修它用的正是本条说的办法:用 `chr(92)` 把反斜杠拼出来。

   **所以第 1 条不是"记住要小心",是"这个动作不可靠,换一个动作"**——知道这个坑的人,
   在专门为这个坑写规则的时候,仍然掉了进去。

   这条和"反斜杠被 Bash 吃掉"同族,但更隐蔽:反斜杠消失会让代码**变形**,控制字符只是
   **变得不可见**——review 里看不出来,复制粘贴照样带着,而语义恰好是对的。

   **第四次,而且是在给这条规则写门禁的时候。** 写 `no-control-chars.test.ts` ——
   一个专门断言"仓库里没有裸控制字符"的测试 —— 那行字符类里出现了**四个真的控制
   字符**,于是**禁止控制字符的那个测试自己带着控制字符**,而它照样能跑、照样绿
   (它扫的是别的文件,不扫自己那一行的语义)。

   **这一次不是工具塌的,而这个区别是整条规则里最容易记错的一处。** 当时用的是 Write,
   第一反应是"连 Write 也吃转义了"。事后用 Write 写探针逐字节验:`\x00`、`\n`、
   `\\`、`\1`、`\s\S\b`、以及那整个字符类,**全部原样落盘**;故意送一个真 NUL,
   也**原样**存下来。它两个方向都不变换。所以是**我自己吐出了那四个字面字节**,而不是
   转义被谁吃掉了。

   **所以这条规则的三次复发都发生在"为它本身工作"的时刻**:写它的注释时、写它的文档时、
   写它的门禁时。这不是巧合——那些时刻恰好是注意力全在"内容对不对"上、而这个坑吃的是
   "字节对不对"。

   ### 检出:门禁,不是"定期扫"

   「定期扫」靠的是记性,和「别用 heredoc」靠的是同一份记性,而那份记性已经失手四次。
   所以它现在是一条测试:`packages/iris-app-service/tests/no-control-chars.test.ts` 遍历
   `packages/** apps/**` 的 .ts/.tsx/.mjs/.md/.css/.json/.yml,断言无
   `[\x00-\x08\x0b\x0c\x0e-\x1f]`,失败信息带 `文件:行: byte N is 0xNN`,允许清单为空
   (真有正当理由的控制字符目前一个都没有;将来要加,得连理由一起写进去)。

   > **typecheck、测试、渲染对这类损坏全部隐形,只有字节层面看得见;grep 之所以找到它,
   > 是因为它报告了自己看不见的东西(`binary file matches`),不是因为它匹配上了什么。**

   ### 同一个症状,两种成因,两种防御

   文件里出现一个裸控制字节,看起来永远是同一件事,但来源有两个,而**换工具只治其中一个**:

   | 成因 | 防御 | 为什么另一种防御无效 |
   | --- | --- | --- |
   | Bash/heredoc 吃掉反斜杠 | **换工具**(Write/Edit) | 再仔细的自查也拦不住一个会变形的通道 |
   | 我自己吐出了字面字节 | **换检查**(写完读回、扫字节) | 换工具毫无作用——用的已经是"安全"的那一个,而**没有哪个工具有机会知道我本来想写的是转义** |

   **两道防线的时机也不同,所以两个都要**:门禁跑在**事后**(一次测试运行,可能是几十次
   编辑之后),字节自查跑在**同一个动作里**。上面那次是自查抓到的,不是门禁——那时门禁
   自己就带着损坏,而它并不检查自己那一行。

   那三处真实损坏各活了数月:两个测试里的 NUL(行为正确,所以永远不会有人去看),以及
   一份文档里 `\1` 反向引用塌成 `0x01`——**那一处印出来的正则和它记录的正则不是同一个**,
   而渲染出来看不出任何异样。

2. **文本替换之后,必须重读文件断言替换落地了。** 断言的是 `count(替换后的样子)`,
   不是 `count(替换前的样子)`。

   实证:把协议里 `messageId` 的 schema 从 `z.number()` 改成接受 `'latest'` 的联合类型,
   **替换静默没生效**,而我只断言了"那个通用行出现 3 次"。于是**注释承诺收 `'latest'`、
   校验器仍然拒绝**,两边都进了仓库。typecheck 过(zod 把类型窄成 `number|undefined`,
   而消费它的函数入参更宽,兼容);943 条测试全绿(**没有一条从 schema 走过这个字段**)。

   `str.replace()` 找不到目标时**不报错**,这是它和 `Edit` 工具最要紧的区别。所以用
   `replace` 就要自己补上 `Edit` 免费给的那个保证。

   **一般形式**:任何"改完就走"的批量操作,验证要落在**改完之后的状态**上,而不是落在
   "我以为我改了什么"上。

3. **运行时要镜像一个类型联合时,用 `Record<联合, true>` 生成,不要手写 `Set<联合>`。**

   ```ts
   new Set<Code>(['a', 'b'])              // 只校验每个元素是合法 Code
   const ALL: Record<Code, true> = {...}  // 少一个键就编译不过
   ```

   **区别不是风格,是它检查的东西不同**:`Set<Code>` 的类型参数保证「列进去的都合法」,
   不保证「合法的都列进去了」。所以往联合里加一个成员时,**编译干干净净,而那张运行时
   表悄悄少一个**。

   实证:契约里加了错误码 `quota-exceeded`,`iris-rpc-host` 的运行时码表漏了它,于是
   `toRpcError` 把它降级成 `internal`。**两端都看不见**——抛的一方看到自己的码,读的一方
   看到一个通用内部错误,而为那个码专门写的界面文案永远不显示。三处测试全绿,因为没有
   一条把码从抛出走到线上。

   **一般形式**:任何「运行时的一份表必须和编译期的一个集合保持同步」的地方,都要选一个
   **少一项就报错**的构造,而不是选一个**多一项才报错**的构造。前者的失败模式是编译红,
   后者的失败模式是**线上悄悄降级**。

   **同一个病有三种语法,第三种最狠。**

   | 写法 | 少一项时 | 多一项时 |
   | --- | --- | --- |
   | `new Set<联合>([...])` | 沉默 | 编译红 |
   | `readonly 联合[] = [...]` | 沉默 | 编译红 |
   | **三元 / if 链** | **沉默,且直接替你决定** | 沉默 |

   前两种至少还要有人**漏写**一项;第三种**连漏都不用**——它本来就没打算列全,只是在
   两个已知值之外给所有人一个默认答案。实证:注入位置的
   `position === 'at-depth' ? … : { order: position === 'before' ? 850 : 950 }`,
   `ScriptPromptPosition` 有四个成员,它按名处理两个,**其余一律进 `after`**。加第五个
   成员时,卡的文本会被放到没人选过的位置,没有编译错、没有报告、没有测试会红。
   改成 `switch` + `never` default 之后,加一个成员立刻是编译红——**改动前加那个成员是
   编译通过的,这就是这条守卫的全部价值。**

   **更准的说法是「少一个决定就报错」。** 把这条拿去查自己域时发现,不是每处都该「必须
   列全」:诊断的 `WIRED_KINDS` 里,「这个 kind 声明了但故意还没接报告点」是**一个真实
   状态**。所以那里用的是 `Record<Kind, boolean>` 而不是 `Record<Kind, true>`——每个成员
   都必须**被写下来**,但可以写成 `false`。**遗漏和「决定不做」长得一模一样,而构造要能
   把它们分开。**

   那一处的后果值得记,因为它比错误码那次更讽刺:`WIRED_KINDS` 正是诊断页用来分辨
   「采集在线但无事」与「这类根本没接」的字段。漏一个 kind → 页面告诉用户「没人在看」,
   而它的报告正在到达——**这个字段存在就是为了防这句谎,而它自己制造了它**。

   **同族的第三种形状:联合上的 `switch` 没有 `default`。** 五个成员全覆盖时它编译通过、
   行为正确;加第六个成员,命令会被提取、被计数,然后**既不执行也不拒绝**——静默丢弃。
   修法是 `default` 里放一个 `never` 赋值(加成员即编译红)**外加**一条具名拒绝(运行时
   真来了也出声)。三种形状同一个病:**多一项才报错的构造,失败模式都是线上悄悄降级。**

   **最后一条,关于怎么用这三条:把规则当成「去查一遍」的动作,不是「以后注意」。**
   上面第二、第三处都是在为第一处写完规则**之后**才找到的,而且都在我自己写的代码里。
   若只把它记成一句提醒,它们会一直待在那儿——**写完一条规则的下一个动作,是拿它扫一遍
   自己的域。**

4. **`node --test` 全绿不等于能构建。类型剥离不做类型检查,两个命令都要跑。**

   `node --test --experimental-strip-types` 是把类型**擦掉**,不是检查它们。一个
   带真实类型错误的测试文件照样运行、照样通过;只有 `npx tsc --noEmit` 看得见。

   实证:新写的一条测试里 `new ChatEntry({...})` 漏了必填的 `card` 成员。**套件
   报 974/974 全绿,`tsc` 在同一个文件上报错。** 先跑完的那个、看起来最让人放心
   的那个,回答的是另一个问题。

   这条和第 1 条同族:**门禁绿着,动作却没成立。** 差别在于这次绿的是测试套件
   本身——所以"测试通过"在这个仓库里**不构成**完成证据,证据块里两个都要引。

### 机械动作:凡是"某类有 N 个",把那 N 个逐条打出来看一眼

前面几条都是纪律,这一条是动作 —— 因为纪律在同一个窗口里失效了四次,而这个动作每一次都能救。

> **一个"某类有 N 个"的结论,在 N 小的时候,打印那 N 条几乎免费;而它是唯一能同时抓住
> 判据太宽、判据太窄、和判据解析错的动作。**

同一窗口里的四次,全是我的:

1. **"片段带 46 楼"** —— 宽判据要求 `<div class=…>` 或 `<style>`/`<script>`,而这个带的主力标签是
   `details`/`summary`/`li`/`span`/`p`。真值 **936**,漏了 890。判据太窄。
2. **"6 楼是裸文档"** —— 三反引号扫描器遇上 ` ````html `(四反引号)和 ` ``` ` + ` ```html `(双开括),
   把配对算错,于是文档标记落在"所有围栏之外"。逐条读那 2 个楼层位置(6 个 file-instance)才看清:
   它们是**模型把输出格式要求背进了正文**,里面那份文档是模板。判据解析错。
3. **"会漏出去的选择器 0 个"** —— 过滤名单写的是 `body|html|*|p|div|span|table|li|ul`,**没写 `details`**,
   而那 4 个正是 `details summary::marker` 一类。真值 **4**。清单只能谈论它上面有的名字。
4. **"7 个成员没建"** —— 用一个宽松的 `'name':` 正则扫了整个 `identity.ts`,而正确的来源是
   `MEMBER_KINDS` 块里标了 `identity`/`shared` 的条目。`eventOnce`/`getLastMessageId`/`substitudeMacros`
   本来就built。真值 **4 个没建**。判据的**来源**错。

四次里有三次是"数出来的东西不是我以为的那类",一次是"来源取错"。**共同点是结论都只有一个数字,
而那个数字看起来完全合理。** 打印明细之所以有效,不是因为人比正则聪明,是因为**明细把判据的产物
摊开成可被认领的具体条目**——`_gender` 是谁?`fin` 是什么字段?第 54 楼为什么在这里?这些问题
只有在看到条目时才会被问出来。

配套一条给报告的:第 4 项那种错(来源取错)**在输出里完全看不出来**,因为它产生的是一份格式正确、
数量合理的名单。它只能靠**和另一个已有结论对账**发现——那次是我记得 `eventOnce` 在之前的普查里
是"已建但未用",两个结论对不上才回头查。所以**同一个事实被两次测量报出不同的值时,先别选一个信,
先问两次测量的来源是不是同一个**。

**第五种,由协调者指出:类别对、施加的对象错。** 同一窗口的第五次——`is_frontend` 这个谓词是对的,
但我把它跑在**整段渲染文本**上,而上游跑在**已渲染 DOM 里 `<pre>` 元素的文本**上。数字从 189 变成
179,判据一个字没改,只是换了施加对象。而这次**明细完全正常**:189 条里每一条都真的含文档标记,
逐条打出来看不出任何可疑——因为它们确实属于我说的那一类,只是那一类不是上游在分的那一类。

所以这条动作要连对象一起说:

> **打印"某类有 N 个"的明细时,把"这一类施加在什么对象上"一起打出来。**

前四次是类别错,明细能救;第五次是对象错,**明细救不了,只有把对象写进结论句才能被别人看见**。
这也是为什么那半句必须进结论、不能只留在脚本注释里:注释只有写它的人会读,而对象错**恰恰是
写它的人看不见的那一种**。

### 陈列方式会改变证据的表观强度

前面几条讲的是证据本身有多少鉴别力。这一条讲**同一份证据被摆成什么形状**——因为形状
会独立地改变读者估到的强度,而作者通常察觉不到自己在放大。

**第一面(3c 指出):一个前提的多个推论并排列出,读起来像多个独立证据。**

一条静态推断(overlay iframe 的 `window.parent` 是宿主 window、监听却挂在脚本 frame 的
window 上)带出三个推论:`document.body.contains` 恒假、`observe(document.body)` 观察空文档、
`resize` 监听在隐藏 frame 上不触发。**这不是四个发现,是一个前提加三个后果**——把它们
并排写成四条,读者会当成四份互证。3c 的做法是写成一组并注明「若那条被证伪,这三条一并
作废」。

这和第十节那条「用一个必然正在发生的问题给一个目前是零的修改背书」是同一个毛病的两面:
**证据强度会顺着排版流动**。捆在一起看不见,拆开并列反而更危险,因为并列**看起来**是更
诚实的呈现。

**第二面:一个承重的零,要报成分账,不要报成计数。**

同一轮里,3c 报了「`$('选择器字符串')` 出现 0 次」,而整份文档的结论压在这个零上。我报的
是「传给 `$` 的字符串只有一个,是 `'<iframe>'` ——那是建元素不是查文档;`$` 的 10 次调用里
9 次传的是对象或元素」。

> **一个裸的零,分不清"它不这么用"和"我的探针没数对";一份分账两者都能分辨。**

3c 自己的总结更准:**零是个否定,分账是个肯定。** 而这一窗的经验是,否定结论恰好是最容易
由坏探针产生的那一种(第九节第三面:零是唯一一种全盲检测器也会给出的答案)。所以纪律是:
**凡有结论压在某个零上,把该类的全部命中去向列出来**——`10 = 1 建元素 + 9 传对象` 是可核对的,
`0` 不是。

### 自检版:确认性预测只值它覆盖的分叉

本节前面讲的是两个**来源**之间的印证。同一条定律作用在**一次测量的自检**上时更危险,
因为那里只有一个作者,而通过自检的感觉和尽职的感觉是同一种。

复核 49 的往返实验时,他文档没写口径,我得先猜。我猜的是"删掉 `variables` 后不再
hydrate",并且**专门设了一个地标来验证这个猜测**:「如果我读对了他的口径,31 个 chat
里唯一逐字节相同的会是 `default_Assistant`。」它**成立了**——而我的口径仍然是错的:那个
chat 整份没有 `variables`,在**两种**口径下都是空操作,所以它对两种读法都相同。这个地标
在两条口径**不分叉**的地方,于是它的成立什么都没许可。我据此报出了「1233 行**内容**不同」,
正好是他结论的反面,拦住我的不是自检,是后来去读了他文档里那句「再用原始行照常
`hydrateVariables`」。走真管线重跑后,他的三个数一位不差。

> **信一个预测之前,先说出"什么结果会推翻它",再确认错的读法真的会给出那个结果。**
> 每一种候选读法都满足的地标是装饰品。

而这次真正把口径定下来的动作是去读源码:`st-meta` 不是一个描述,是
`iris-persistence/src/sillytavern.ts` 里 `MODELLED_KEYS` 的补集,**它是这个口径的唯一
来源**——我自己枚举的五种"核心 vs 非核心"分法最接近的给 27.50 MiB,一种都不中,因为
`send_date` 不在 `MODELLED_KEYS` 里所以属于 st-meta、`swipe_id` 在里面所以不属于,跟每一种
猜法都不同。**凡术语指的是代码划定的集合,就去代码里把那张表读出来**;一种合理的划分只是
换了个同名的另一次测量(第十三节)。

**同族的一条,关于针本身:「已知已修」的样本当不了针。**
扫「运行时表镜像字符串字面量联合、但漏项」这个形状时,拿到两个"已知已修"的样本当正例针,
两个都没被扫到。按第九节第三面,能返回 0 的普查必须先证明卡尺看得见东西,所以这看起来像
探针死了。去读源码才知道:**两处都是靠改成 `Record<Union, true>` 修的,改完就不再具有我要找的
形状** —— 没扫到才是对的。

> **针必须是_当下真的具有该形状_的东西。** 一个已修的样本证明的是"这个形状曾经存在过",
> 而扫描问的是"现在还有没有";两者之间隔着那次修复,而修复恰恰就是把形状拿掉。

这次是靠去读那两处源码救回来的,不是靠机制——**而它可以往两个方向坏**:把真零报成"探针
可能死了"(白丢一个结论),或者把探针死了报成真零(第九节那条最锋利的:零是唯一一种全盲
检测器也会给出的答案)。

### 两步推理只查实了第一步

上一条讲地标选错了层。这一条更常见:**推理有两步,你把第一步查得很扎实,那份"已经尽过职"
的感觉正好盖住了第二步。**

量「上游界面 frame 预置了、我们没有的库」时,我发现 6 张卡自己注入 `<link>` 到
`cdnjs.cloudflare.com` 取 Font Awesome。于是两步推理:

1. **「卡自带」只有在宿主放行时才等于「自足」。** —— 我去查了,`REMOTE_ALLOWLIST` 只有
   `*.jsdelivr.net` 和 `raw.githubusercontent.com`,cdnjs 不在;而且 `font-src` 不随授网放宽。
   查得很实,连"授网也救不回来"这种反直觉的结论都拿到了。
2. **「宿主没放行」只有在宿主自己没预置时才等于「坏」。** —— **这一步我没查**,直接当成立了。

第二步是假的:我们自己在 message frame 里预置了 FA 6.5.2,图标由预置供给,卡那条 `<link>`
被拒是无害的。我据此把一整栏写成了「需要我们补 7」,真值是 0。

危险在于第一步查得越扎实,越不会去查第二步——**做过一次防御动作,会消耗掉本该用来怀疑它的
注意力**(第九节那条的另一个位置)。而且两步共用同一个词:「坏」。第一步结论里的「坏」指
*卡自带的那份取不到*,第二步需要的「坏」指*图标画不出来*,**同一个词换了对象**,于是接缝
处看不出来(第十九节开头那条「对象错」的第三次现形)。

> **可操作的形式:把推理写成带前提的两句,然后逐句问"这句的前提我查了吗"。**
> 一条写成一句话的推理没有接缝,也就没有地方问。

顺带一条:纠错时把「命中数」也重查了一遍,发现 7 里有 1 张是假阳性——`\bfab\b` 匹配到了
class 名 `recovery-fab` 里的 `fab`(`-` 是词边界)。而那张恰好是唯一的 SCRIPT-DOM 卡,
**假阳性刚好落在唯一要紧的那一格上**。所以结论被推翻时,**连同它一起报的计数也要重查**,
不要只改结论:两者出自同一次匆忙。

### 假阳性能撤回一条正确的收紧

前面几次坏探针给的都是**零**,危险在于「没人用,可以不做」。这一次方向反了,危险不减。

裁定「`message_id: null` 一律拒绝」之后,我去量语料是否会被打到,探针报**「`null` 出现 2 次」**。
第一反应是「拒绝 null 不是零成本,裁定要重议」。展开看,两处都是那张卡**自己的辅助函数
的返回值**——`return { mk: '', message_id: null, isNewKey: false }`,表示"没能确定楼号",
**从不传给 `getVariables`**。探针匹配的是「`message_id:` 后面跟什么」,
**没有区分"API 选项对象里的键"和"卡自己的对象字面量里的同名键"**。

> **同一个键名,两种语境,只有一种是调用。**

值得单记的是**后果的方向**:假零让人**少做一件该做的事**,而这个假阳性会让人
**撤回一件已经做对的事**——两者都往"少做"推,但后者更难挽回,因为撤回时手里
握着的是"实测数据",比当初下裁定时的论证更像证据。

**判据不是"匹配到名字",是"匹配到调用"。**统计一个 API 的用量时,
探针要锚在**调用点**(函数名、`(` 、选项对象的位置)上,而不是锚在参数名上——
参数名是全世界都能用的词。

### 一个值可不可能是 `undefined`,答案在构造点不在比较点

同族的另一面。上一条讲探针锚错了地方,这一条讲**读代码时锚错了地方**。

我据 MVU 的 `filterEntries` 提过一个机制假设:「设置未加载 → `更新方式` 是 `undefined` →
`undefined !== '随AI输出'` → 走移除路径」。**不成立,而且不成立的证据在我读过的同一个文件里。**

我读的是**比较点**(`filter_entries.ts:43` 的 `=== '随AI输出'`),也读到过**赋值那一行**
(`store.ts:306` 的 `Settings.parse(_.get(SillyTavern.extensionSettings, 'mvu_settings', {}))`)——
**但没有往上翻到构造点**:`:249` 的 `z.union([...]).catch(() => NewSettings.parse({}))`
和 `:134` 的 `.default('随AI输出')`。**三层兜底,那个值永远不会是 `undefined`。**

**漏掉的两层都是"默认值"**:`_.get` 的第三参、zod 的 `.default()`。
它们的共同点是**在读取处不可见**——`_.get(a, b, {})` 那个 `{}` 混在参数里,
`.default()` 在另一个文件的 schema 里。**"这个值可能是 undefined"是关于它怎么来的判断,
而我拿它怎么被用的地方去判断。**

> **要断言一个值可能缺席,去读它的构造点;要断言它必然存在,同样去读构造点。
> 比较点只告诉你缺席时会怎样,不告诉你会不会缺席。**

代价方向和上一条一样:这个假设**制造了一次不存在的风险**,并差点让一条账本条目
从"待支持的功能"升格成"待监控的故障"——**两者的对账动作完全不同。**

### 账本条目按"读者会去核对什么"写,不按"感觉偏离多少"写

上面那句"两者的对账动作完全不同"值得单列,因为它在一天里判了四次。
**账本条目的读者不是在读结论,是在决定下一步做什么。**

| 写成 | 读者会去做 | 实际该做 |
| --- | --- | --- |
| 「我们**窄一级**」 | 去找我们比上游少覆盖的场景 | **那个场景不存在**(上游两条机制覆盖面互相包含)——正确写法「**等价,机制少一条**」 |
| 「我们**改了规则**」 | 逐条比对两边规则 | **规则完全相同**,少的是上游那条多余的判存在路径——正确写法「**少一个不一致**」 |
| 「五个回调**不跑**」 | 印证"当前一切正常",合上账本 | 默认模式下确实无害,**切到另一模式才是故障**——要写成「**不跑,且在 X 模式下会怎样**」 |
| 「**待监控的故障**」 | 加监控 | 触发条件只能由人显式做 ⇒ 是「**待支持的功能**」,等有人要用再建 |

**四次都不是"更委婉"和"更严厉"的区别,是指向不同核对动作的区别。**

**第四条附带一个写法约束:否定句要带代价。**
「这不是另一种权衡」是个否定,读者会接着问"那代价是什么";
不给代价,这句就读起来像风格判断,**而风格判断没法复核**。
那次的完整写法是「**同一个语义用错了单位,不是另一种权衡——恢复点少一半的代价是回溯更长**」。

*(收拢记录:这四条此前散在消息与各自的账本里,METHODS 只有上一段那半句。
已核 `DEVIATIONS.md`——「核对动作 / 对账动作 / 会去核对」零命中,**那边没有平行的表**。
**本条是唯一一份**;`DEVIATIONS.md` 头部指向这里。一处规矩、一处引用。)*

### 两条路径一致而你仍不放心时,别找第三条同类路径

本节开头那条("一致只在两条路径真正分叉的那一层上有鉴别力")讲的是**怎么判断一次印证值多少**。
这一条讲**判断出它不值钱之后该做什么**——而直觉给的答案是错的:**再找一个人复核一遍。**

**实例。**我报过覆盖层卡的一条 realm 不一致:「overlay iframe **内部**的 `window.parent`
是宿主 window,而监听在脚本 frame 的 window 上,两端不在同一个 window」。
44 独立复核并确认,走的是另一条路径——**读 append 目标与 listener 的绑定对象**,
和我读 `$` 的 realm 绑定不是同一条。**两条路径,一致。**

**而它是错的。**两条路径**共享了同一个前提:「那个嵌套 iframe 里有代码在跑」。**
没有。**它的 srcdoc 里没有 `<script>`**——整个嵌套文档是纯 DOM,
`postMessage` 那行位于组件的 setup 闭包里,**是卡 frame 的代码**。
正确形状是「同一个 realm,一个 post 给 `parent`、一个听 `self`」。
(结论"消息到不了"侥幸不变;机制全错。)

**拆穿它的不是第三次复核,是问了一个它们都没问过的问题:「那个 srcdoc 里到底有什么」。**

> **同类路径会把共享前提一起带过去,复核次数再多也不增加对那个前提的检验。
> 要检验前提,得问一个不需要它就能回答的问题。**

**怎么找那个问题:把两条路径的推理写成句子,划掉它们都用到的名词,
剩下没被任何一条查过的那个,就是要问的。** 这次是「嵌套 iframe」——
两条路径都在推理**它内部**如何,**没有一条查过它内部是什么**。

### 判"多余"之前,先问它服务的是现在的决定还是将来的决定

MVU 清理规则修完后,那条对照是「重算的幸存集合 `deepEqual {0,50,…,650}`」。我发现了一件
事并且**自己判定它多余**:那份期望值是从**已被 ST 清过的文件**里读出来的,所以对照证明的是
「我们与 ST 在同一输入上一致」,**不是「清理规则本身正确」**。当前目标就是兼容,两者等价,
所以我报了一句"这句我没写进文档,因为对当前目标是多余的"。

**总指挥判它不多余,理由是我没想到的那一层:它不回答"现在要不要担心",它回答
"将来谁要改进清理规则时,该先动哪条对照"。** 将来那个人一改规则,我们必然与 ST 不一致,
**这条对照会把他的改进报成回归**;没有这条脚注,他会先怀疑自己写错了。

> **多余的是那句_陈述_,不多余的是它的_用途_。** 我把两者合并成一句,于是把对
> **当前决定**无用,直接判成了对**所有决定**无用。

**这不是"多写一点更保险",那会把文档写成流水账。** 分界是可操作的:

- 服务**当前决定**的信息:决定做完就该沉进代码或测试,文档里留着是噪音(第七节)。
- 服务**将来决定**的信息:**它标注的是"这条约束哪天会失效、失效时先动哪里"** ——
  这种句子代码里放不下,而它恰恰是下一个人最需要、也最不可能自己重建的。

**一个筛子**:问「**这句话是给谁读的**」。答案若是"现在的我"→ 多半可以不写;
若是"**将来要改这块的人**"→ 写,而且要写清楚**他会看到什么现象、那现象为什么不是他的错**。
在验收单、账本这类**会被反复读、且读者带着不同目的**的文档里,后者常常比前者值钱。

## 二十、一条报告有三个字段是"它在哪儿说的"

内容对、**位置错**的报告,是这一天里最贵的一类。三种位置各栽一次,形状同一个:
读者先读位置,再读字。

### 通道是报告的第一句话

`reportGap` 对全部十七个调用点都 post `type: 'error'`,而它自己的契约头一句写着
**"Not an error: the card carries on"**。文档是对的,接线是错的,所以**没有任何一处
自检能抓到它**——两边都在,只是不曾对账。

面板按通道分组,读者因此在读到字之前就已经读了通道。于是:

```
1 of 2 loaded, 1 still starting.        ← 真的标题
localStorage is not available …         ← 真的句子,但在 failed 栏
indexedDB refused to open …             ← 真的句子,同栏
```

**两句真话加一个真标题,合成了一个假的因果故事**,而合成是通道干的。3c 后来证伪了
那条因果(MVU bundle 零引用存储),但在那之前它已经害两轮排查顺着存储查下去。

拆法不是加一个 channel 参数,是**两个名字**:`reportGap`(note)/ `reportFault`(error)。
理由是**这个选择是一个关于发生了什么的主张,名字应该在调用点就把它说出来**。判据不是
严重程度,是**这次调用有没有被服务**:

- 畸形按钮行会**整表拒绝、什么都不存** → fault,即使没抛;
- 未建成员读回 `undefined` → note,即使卡三步后死于它。

同族的还有 `toastr`:`toastr-report.ts` 自己的注释写着"a report list that flattened
them would make a routine notice look like a fault",然后把 **channel** 压平了——
**在文本里点名级别,在通道里抹掉它**,是同一个错误往下一层重演。

### 报告要说清是哪一层拒绝的

`CARD_METHODS` 从第一天漏了 `replaceScriptButtons`。拒绝发生在 `client/store.ts` 的
门口,而 `writeButtons` 把 rejection 包成
**「the host refused to store the table」**——于是一条本层做出的拒绝,被当成宿主对一个
它从没收到的请求的回答呈现给读者。

**代价可以计量:两个会话跨三个提交核了宿主的 handler、注册与契约,才有人回头查任一名单
是不是漏了一行。**

改法两处:门口的文案改成「**this app** does not list it … the host was never asked」,
包装句从「the host refused」改成「the write was refused」。规则:

> 报告默认署自己的名。要说"是别人拒绝的",得有能区分的证据。

### 按名字写的断言放不出错投

ST 面上凡是没有自己分支的成员,都掉进同一个 fallthrough,而那个 body 无论读到什么名字
都调 `callAction('generateRaw', …)`。也就是说 `SillyTavern.generate('hi')` 一直在跑
`script.generateRaw`——收 `prompt`、**不带聊天历史**,而 `script.generate` 收
`userInput`、带历史。

那条 ST 面 pin 断言的是成员**名字**,`assert.deepEqual(Object.keys(surface), [...])`。
**错投恰好能通过一条按名字写的断言**:名字全在,路由全错。678 条测试里没有一条能抓到它。

> 断言的对象是名字,漂移的是路由。凡是"表驱动"的分派,测试要钉**每个名字送到哪儿**,
> 不是"这些名字都在"。

现在钉的是 `['generate','generateRaw']` 两个方法名**和** `userInput`/`prompt` 两个字段名。
与「齿检假设夹具」同族:两者都是断言方向与漂移方向**正交**,所以再多的样本也不增加判别力。

### 补 §十七:第二道守卫在哪里可以被观测

§十七「两层守卫的保证无法归因」记的是"删掉任意一层测试都还绿"。这一天里同一形状**又付了
两次**,而我两次都没想起这条已经写下来的东西——所以先记这件本身:**写进 METHODS 不等于
下次会想起来**,而重复付费的形状值得在条目里留下**可被搜到的关键词**(这里是
`detachReturns`、`structuredClone`、"第二道拷贝")。

两次的收获是 §十七没说的那一半:**存在一个它可以被观测的地方。**

1. **楼层读**里我在 `floorVariables` 又 spread 了一份表。变异"按引用返回"→ 测试**全绿**,
   因为 `detachReturns` 已经 clone 过。→ 多余的拷贝删掉,测试改瞄真正持有这条性质的那一层
   (拆掉 `detach` 的 clone → 红,验过)。
2. **`injectPrompts` 的句柄**要豁免 clone(`HANDLE_RETURNS`)。变异"移出豁免名单"→ 测试
   **全绿**,因为 `structuredClone({uninject: fn})` 会**抛**,而 `detach` 的兜底把原对象
   交回去了。值两种情况都活着。

第二个例子给出了通用答案:**当一层已经无条件提供了某个性质时,新加的守卫在"值"上不可观测,
但常常在"报告"上可观测。** 那条兜底会说
"returned a value Iris could not copy, so the card holds a live reference"——对一个契约
就是活引用的成员来说**这是误报**,而误报正是豁免名单存在的理由。断言改成
「值可用 **且** 没有 could-not-copy 报告」之后,变异红。

> 一个观测不到的第二道守卫比没有更坏:下一个读者会把它当成起作用的那道,然后不再往下看——
> 而那正是这条性质坏掉时最不该看的地方。

所以三选一,按顺序试:① 断言它可观测的那一面(通常是报告);② 观测不到就**删掉它**,并在
注释里写明保障来自哪一层;③ 两者都不行才留着,并像 §十七那样写明"删掉任意一层它还是绿的"。
## 二十一、拟合出来的比值,推不到没被拟合的那一头

一份要决定「值不值得拆」的设计输入里,我要估两个数:**可搬走多少、留下多少**。

手上没有直接的数,于是我拟了一个比值。当天有四个**实测**增量:`st-anchors` 14 KB 源
→ +5 KB 构建、`card-storage` 17 → +4.2、`overlay-regions` 8 → +1.7、`injectPrompts`
≈4 → +1.2。四个点都落在「构建 ≈ 源 / 3」附近,看起来相当稳。据此我报:

> 可搬的大约 45–55 KiB,策略核落在 10–20 KiB。

**真实是可搬 27.6 KiB、核 40.1 KiB。核大约是我估的两倍。**

### 为什么拟合会错在这里

那四个点**全是叶子模块**——新写的、注释密度高、逻辑薄。而占大头的
`frame.ts`(94 KB 源)和 `frame-entry.ts`(67 KB 源)是**主干**:代码密度高得多,注释
占比低得多。用叶子的「源/构建」比去推主干,等于假设两者的注释密度相同,而**那正是它们
最不同的地方**。

> 一个从同质样本拟出来的比值,只在同质的那一段有效;而人要估的往往正是**异质的那一端**,
> 否则就不必估了。

### 更要紧的一条:它本来就能直接量

拆开成两个 `vite build` 读两个产物大小,**一分钟**。我没量,是因为「有个比值可用」看起来
已经够。而那份设计输入里**唯一承担论证重量的数字**(核落在 10–20 KiB,所以拆分划算)
正是错了一倍的那个。

**判据:一个数如果是论证的支点,就不能是拟合来的——除非拟合比测量便宜得多。** 这里测量
几乎免费,而我付了一次「结论对、论据错」。结论侥幸没变(拆分仍然划算,因为主收益是
**增长率**而不是省下的字节),但那是运气,不是方法。

### 附:主收益常常不在被估的那一栏

我估的是「省多少字节」。拆完之后真正改变局面的是**每加一个成员不再乘以帧数**——
拆分前每个新成员付 12–20 遍,一天之内把数量闸从 20 压到 12;拆分后它完全不动那个常数。
省下的 24 KiB/帧是一次性的,增长率是永久的。

**估算时先问「这件事改变的是水平还是斜率」**:斜率的改变通常不出现在你正在估的那一栏里,
所以只估水平会让一件该做的事看起来只是勉强划算。
## 二十二、隔着一条没测到的缝相对而立的两个测试

给一个字段做端到端,我测了生产端(`describeBlocked` 会返回 `covered`)也测了消费端
(面板会把它渲染成 note),然后交付了一个**从来没有通电过**的功能:两者之间那行手抄字段
进消息的代码在 `frame-entry.ts` 里,我加了协议、加了 runner、加了壳侧渲染,**唯独没改那行
手抄**。于是字段被算出来、被丢掉,两个测试一路绿,我还带着它们的输出报了「做完了」。

> **两个隔着一条没测到的缝相对而立的测试,证明力小于一条跨过那条缝的测试。**

缝两侧的覆盖看起来像是对整条路径的覆盖,这是它骗人的地方。而那行手抄恰恰待在
**入口模块**里——`node --test` 加载不了 `.tsx`,也加载不了带副作用的 entry,所以这类
「只是搬一下字段」的代码会**专门在测不到的地方堆积**。不是巧合:它们之所以在那里,正是
因为那里没有测试的压力。

### 修法不是补一行,是把组装搬进被测模块

补上被漏掉的字段,下一次加字段还会漏。真正的修法是让「往报告里加字段」和「往消息里加
字段」**成为同一次编辑**:消息组装移进已经被测的那个模块,入口里的监听器缩成一句
`post(blockedMessageFor(run, event, shellOrigin()))`,只剩它独有的两件事——事件,和我们在哪。

而那条跨缝的测试要**遍历生产端自己的输出**,不要手写字段清单:

```ts
for (const [key, value] of Object.entries(decided)) {
  assert.equal(message[key], value, `the message dropped ${key}`)
}
```

因为失败模式就是「忘了一个字段」,而**手写字段清单的是同一只手**。牙齿也别自己发明:把
真正 ship 出去的那个 bug 原样重放(删掉 `covered` 那行),看它是否恰好红。

### 后续:那个 map 上任第一天就抓到一件

这一条的修法之一是把事件分发从一串 `if` 换成 `Record<IrisEvent['type'], handler>`,
理由写的是「少一个决定就报错」。**当天它就报了一次**:49 往协议里加了 `cleanup.offer`,
我在跑无关验证时 tsc 直接停下并点名 `Property '"cleanup.offer"' is missing`。

对照上一轮同一件事是怎么被发现的:`report` variant 加进来时,分发里没有它的分支,
**没有任何东西会抱怨**——它之所以没被静默忽略,只因为分支上方那行**恰好**读了
`event.chatId`,而那个 variant 没这个字段。

> 一次靠意外,一次靠类型。**同一个缺口,两种代价。**

判据:凡是「新增一个 variant 就该有人做决定」的地方,让**缺少决定本身**成为编译错误,
而不是指望某条恰好会碰到它的代码。代价是一次重构和一句 `as` 转换(TypeScript 不会把
`HANDLERS[event.type]` 与 `event` 关联起来),换来的是这类遗漏从「靠运气发现」变成
「不可能提交」。

### 同族:构造点对了,比较点错了

同一批里还有一条同族的:`blocked-report.ts` 的自源分支用 `location.origin` 做比较,而
沙箱 srcdoc 帧的 `location.origin` 是**字符串 `"null"`**,所以那条分支从写下来那天就
不可能命中。模块的文档注释里写着「`blocked 127.0.0.1:8787 (connect-src)` 是唯一一句
无法区分这些情况的字符串」——它一直在生产那句话。**机制是对的,输进去的是 `"null"`**。

> 一个从不命中的分支和一个不存在的分支,在测试里长得一样;区别只在它**声称**自己存在。

判据:凡是拿「我自己是谁」去做比较的代码,先问**那个值是谁给的**。不透明源、代理后的
主机名、容器里的端口——这些地方 `self` 一类的值全都不是它看起来的东西。

## 二十三、帧内几何:读之前先验证读数是否成立

机械规则,先写结论:

> **读任何 iframe 内部的几何之前,先读它的 `document.documentElement.clientHeight`。
> 如果是 0,整组读数作废——不要往下解释。**

2026-09-03 实测:标签页 `visibilityState === 'hidden'` 时,沙箱 srcdoc 帧里一个显式
`height:200px` 的 div,`getBoundingClientRect().height` 读到 **0**,帧的
`documentElement.clientHeight` 也是 **0**;而**同一时刻**顶层页面量得完全正常
(div 200、`innerHeight` 1353)。原因是两者的布局由不同东西驱动:顶层文档被强制读取时会
**同步布局**,而子帧的渲染生命周期在隐藏时被节流,根本不跑。

还有第二个、独立的 0:**视口外**的跨源帧会被 render throttling 挡掉(所以探针帧不要放
`left:-9999px`,要放进视口)。两个原因都给 0,而 0 是最容易被当成「量到了一个真值」的数。

### 这条规则值一整轮

它解释了此前那一轮**自相矛盾的读数**:同一段代码先给 `client=2498x1353`、后给
`client=0x0`,我们两边各自逐条排除了 clip-path、帧定位、`document.hidden`,而真正的变量是
**合成器有没有在跑**,不受任何一方控制。当时正确的动作是「停下来报告不一致、拒绝按戏剧性
的那个读数施工」;现在有了一行前置校验,不必再靠那种自律。

> 一个仪器最该先回答的问题不是「值是多少」,而是「现在这个仪器成立吗」。
## 二十四、可见面偏向那些不可能显示这个 bug 的样本

一条改写把卡的远端 import 指向宿主代理。改写本身对的,代理也对——**除了它顺带换掉了
模块的 base URL**。jsDelivr 的 `+esm` 包体用**根相对**说明符引它自己的依赖
(`import{…}from"/npm/vue@3.5.41/+esm"`),根相对按「谁发出这个模块」的源解析:从
jsDelivr 发出时对,从我们发出时解析到 `http://our-host/npm/vue@3.5.41/+esm`,404。

> 而**自包含的包体没有嵌套说明符**,所以代理对它们完全正确。

于是可见面长这样:MagVarUpdate 的两个镜像照跑(0 报告),唯一有依赖的那个包体死了。
四个脚本里三个正常,失败的那句还是 `Failed to fetch dynamically imported module:
blob:null/999a9a41-…` —— Chrome 在**模块自己的静态 import** 失败时也报顶层 blob,所以
错误信息指向卡自己的代码,不指向依赖。

**三重误导叠在一起**:成功的样本占多数、失败信息指错地方、而机制看起来被大多数样本
验证过了。

### 判据

> 一个只在样本具备某个性质时才发作的 bug,会让**不具备**那个性质的样本继续正常工作,
> 而那些样本恰好构成了「机制是好的」的证据。

所以看到「三个好一个坏」时,先问的不是「那一个有什么毛病」,而是:

**「好的那三个,是不是恰好都不会走到出问题的那一步?」**

这次的答案是「是」——它们没有依赖,所以从不解析嵌套说明符。问出这句话之后,定位只花了
两次 curl:取代理 URL 看包体(里面三条根相对 import),再逐个 curl 那三条(全 404)。

### 与 §十九「判别力的第五种」的关系

那一条讲的是两个测量在哪一层分叉;这一条讲的是**样本本身在哪一层同质**。同族,方向相反:

- 前者:两个来源都对,但它们**共享**的那一层不产生判别力;
- 后者:多数样本都好,但它们**缺少**的那个性质才是判别力所在。

两者的共同处置是一样的:**不要数,去问「这个样本能不能显示这件事」。** 一个不可能失败的
样本通过了,不是证据。

### 附:注释里少的那一条,就是后来的代价

`bundle-proxy.ts` 的模块注释原本列了三条「不能丢的性质」,列得很仔细,**少了第四条**
(改写移动了模块的 base URL,所以宿主必须改写它发出的包体)。少的那条正好是后来出事的
那条。这不是巧合:一份「不能丢的性质」清单,是作者当时**想到了**的性质的清单,而出事的
总是没想到的那条。清单的价值不在于完备,而在于**出事后能立刻在同一个地方补上**——所以
补的时候要连「它是第四条、之前不在这里、代价是什么」一起写下来,否则下一个读者会以为
四条就是全部。
## 二十五、只修一半,可能比两件都不做更糟

给说明符遍历加「跳过字符串/模板/注释」,是为了修 32 条误报(散文里的 `from "…"` 被当成
说明符)。真包体上误报从 23 降到 0,看起来是纯赚。

**而另一个包里一条早就存在的测试变红了。**

原因值得逐步写下来,因为它不是疏忽,是**机制上的**:

- 修改**之前**,走查按 token 锚定,只找 `import` / `from`。`const re=/["]/g` 里那个引号
  它**根本不在意**——那只是一个字符。
- 修改**之后**,走查会**停在那个引号上**,把它当成字符串开头,一路找到文件里下一个 `"`
  ——也就是紧跟其后那条 import 的开引号——然后把整段当字符串跳过。

于是:**修掉一类误报,在一个此前是对的位置上造出了一个漏报。**

> 一个不完整的上下文识别器,比没有识别器更危险:没有识别器时它对上下文一无所知因而
> 一视同仁;半个识别器会**自信地**把它认错。

所以正则字面量的识别不是「顺手再加一点严谨」,而是**前一半的成立条件**。补上之后
(前一个有效字符消歧 + 字符类跟踪)真包体复量:14 条真说明符、0 误报,与只加字符串跳过时
的误报率相同,而回归消失。

### 判据

**「现在它也能处理 X 了」之后,先跑旧的那套测试。** 不要假设改进是单调的。这次救回来的
不是我的自觉,是另一个包里一条我没在看的测试——而那条测试之所以存在,是因为**上一版**
走查也在同一个地方踩过一次(它的注释就写着 regex 里的引号会让配对错位)。

> 一个模块被同一个坑绊倒两次,第二次往往长得完全不像第一次。第一次是**配对错位导致漏
> 改写**,第二次是**上下文误判导致漏改写**——症状同一个,机制换了一层。所以旧测试的价值
> 不在于它覆盖了什么代码,而在于它**记住了这个模块会怎么错**。

推论:删掉一条「看起来在测老实现」的测试之前,先问它记住的是**代码**还是**失败模式**。
前者可以随实现走,后者是资产。
