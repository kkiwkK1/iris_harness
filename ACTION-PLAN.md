# ACTION-PLAN — dev/iris-exploration 分支的修改计划

6 张真卡（`测试用卡/`，9 个文件）在 8790 宿主上的实测驱动的执行计划。基线：主线
`dev/iris-exploration` @ `d5d0418`（同源 fetch 桥 + `document.head` shim + 高度回声
修复 + 接口帧 Mvu 表面 + 帧开销 45 KiB），1939 测试 0 失败。四路代理并行，worktree
隔离（`wt-lights`/`wt-qa`/`wt-import`/`wt-plugin-check`），交付后由协调者统一验收合并。

## 一、在途（按合并顺序）

### C. overlay 不可见 + Lights ON 问候帧回归 —— `wt-lights`，最高优先级
- **症状 1**：不要被神隐挑战的论坛 overlay 挂载（srcdoc 45 KB、1449×1294——宽于视口，
  尺寸疑似挂载时量错未更新）但 clip 停在 `'M 0 0 0 0'`，整帧被裁 = 不可见 + 点击穿透。
  引擎本身活着：6 条 prompt 注入持续生效，STATE 面板的变量树完整。
- **症状 2**：Lights ON 问候楼（```html 文档 30 189 字符，数据完好）不再渲染消息内
  iframe；合并前渲染。
- **方法**：二分——只合任务 A（`f22c0b7`）/ 只合任务 B（`999dbdf`）定位引入侧；
  两症状疑似同根（合并后帧几何/可见性链路）。
- **完成判据**：8790 上论坛可见可进、Lights ON 帧重现且内容完整；全绿；真卡前后对照。

### F. 插件检测误报 —— `wt-plugin-check`
- 多卡自检弹"请先安装酒馆助手/MVU"阻断进入，而沙箱已实现这些 API 面。定位卡的检测
  信号（`TavernHelper` 全局探测 / extensionSettings 键 / version fetch / 等待超时），
  对照 ST+已装扩展真值，在探测点给一致答案。涉及神隐挑战、尸变纪元、绿茵好莱坞。

### E. 导入兼容（9/9） —— `wt-import`
- 两张 `000xx-*.png` 字节级只有 SD 生成参数、无卡 chunk：ST 收作空白角色，Iris 具名
  拒绝。`liwy.jpg`：JPEG 卡（COM/APP 段藏数据）Iris 完全不认。先核实 ST 1.18.0 对
  两种形状的接收行为，再对齐；上游也拒的才保留具名拒绝。

### D. 六卡回归验收 —— `wt-qa`
- 全量体检（导入/脚本/真对话/MVU/渲染稳定性/诊断），产出 `QA-REPORT.md`；小修直改，
  大问题只记录根因假说与复现步骤。

## 二、已知未解决（不在当前四路内，排下一批）

1. **真模型生成 15 分钟未返回一次**（deepseek-v4-flash，一次性观察）——先复现：是
   端点慢、还是宿主流管线挂起。挂起则属 `iris-turn`/`llm-openai-compat` 缺超时与
   具名失败，须修。
2. **脚本帧 MVU bundle 25 s 未发布且无代理取数记录**（任务 B 交接注）——与 C 的
   overlay 问题可能同链路，C 的结论出来后归并处理。
3. **zeoseven 字体域被 CSP 拒**（任务 B 交接注）——决定：加远程字体 allowlist 条目
   （按 SANDBOX.md 的字体默认放宽先例）还是记为已知偏离。
4. **状态面板只显示最新 turn 的变量**——长对话回看历史楼层的变量归属（message 作用域
   按候选存储）是否需要在 UI 可回溯，待用户反馈。
5. **webp 角色卡**：语料中已删，但 ST 支持 webp 卡；导入面是否补，随任务 E 的上游
   核实一并定。

## 三、集成纪律（对四路交付统一执行）

1. 逐分支验收：不看代理的自述报告下结论——独立跑其新增/触碰的测试、查禁改文件
   diff 为空、真卡复测。
2. 合并顺序：C（frame.ts 高风险区，先落）→ F（若与 C 同区，rebase 到 C 之后）→
   E（packages 层，独立）→ D（报告 + 小修）。冲突由协调者解决，DEVIATIONS 编号
   重排照 `3c2cb5b` 先例。
3. 每次合并后全量 `pnpm test` + `pnpm typecheck` + `build:web`，bootstrap 字节数
   变化按 `frame-budget.ts` 守卫自己的规则处理（45 KiB 是当前线）。
4. 全部落地后：主检出重建，8790 重启，六卡 + 3 个新导入文件在浏览器人工复验一遍
   再回报用户。
5. 全程：禁按进程名/端口杀进程；主检出与运行中的宿主神圣不可侵犯。

## 四、回归主队列

本分支的六卡兼容收口后，回到 `ROADMAP.md` 的既有队列（设置面等）。本计划的全部
产出与教训（帧几何链路、检测信号真值表、导入行为矩阵）按惯例沉淀进对应文档：
SANDBOX.md、DEVIATIONS.md、METHODS.md。
