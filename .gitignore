node_modules/
dist/
*.tsbuildinfo
.env
.env.*
!.env.example

# Reference checkout of deepseek-harness: read-only, not part of this project.
.reference/

# Test credentials. Never commit.
key.txt
*.key
secrets.json

# Runtime data: imported cards, chat logs, settings. Belongs to whoever runs
# the app, never to the repository.
data/
测试用卡/
qa/results/

# Headless-acceptance screenshots and probe state: evidence for a round, not
# source. The scripts that produce them are committed.
qa/shots-*/
qa/*-state.json
